# GitHub 上传检查清单

上传前确认：

- [ ] 仓库根目录直接包含 `app.py`，不是双层文件夹。
- [ ] 已上传 `requirements.txt`。
- [ ] 已上传 `src/` 文件夹。
- [ ] 已上传 `data/indicator_config.xlsx` 和 `data/macro_series_template.csv`。
- [ ] 没有上传真实客户数据、产品持仓、赎回测算、内部月报或 Wind 导出敏感数据。
- [ ] 如果仓库包含工作相关内容，优先设为 Private。

Streamlit Cloud 部署时填写：

```text
Main file path: app.py
```
